# 📘 **GSI FLASH ҚҰРАЛДЫҚ НҰСҚАУЛЫҒЫ (ODIN – Windows ЖӘНЕ HEIMDALL – Linux/Mac)**

Бұл нұсқаулық Project Treble (GSI) қолдайтын және жүктеушісі ашылған барлық Samsung құрылғыларына жарамды.

---

# ⚠️ **Ескерту**

**Барлық әрекеттер сіздің өз тәуекеліңізде жасалады. Бұл нұсқаулық авторы құрылғыға келетін зақым, деректер жоғалуы немесе bricking үшін жауапты емес.**

---

# ✅ **1. Талаптар**

- Жүктеушісі ашылған Samsung құрылғысы  
- Windows немесе Linux/Mac ПК  
- Odin (Windows үшін)  
- Heimdall (Linux/Mac үшін)  
- Samsung USB драйверлері  
- USB кабель  
- **super.img файлы бар архив** (және boot.img, егер бар болса) — жүктелген GSI  
- Ұсынылады: телефон толық зарядталған болу керек  

> ⚠️ GSI жүктегенде сізге **архив** беріледі. Оны **толығымен ашу қажет емес**, тек архивтегі **super.img** (және boot.img, егер бар болса) файлын флеш жасау қажет.

---

# 🧰 **2. Құрылғыны дайындау**

## **2.1. Developer Options қосу**

1. Параметрлер → Құрылғы туралы  
2. **Build number** элементін 7 рет басыңыз  
3. Developer Options мәзірінде қосу:
   - **OEM Unlocking**  
   - **USB Debugging**

---

## **2.2. Bootloader-ді ашу**

1. Құрылғыны өшіру  
2. **Volume Down + Volume Up + USB қосу** түймелерін басу  
3. **Volume Up** басып растау  
4. Құрылғы деректерді өшіреді және bootloader-ді ашады

---

## **2.3. Деректерді өшіру**

1. Recovery режиміне кіру (**Volume Up + Power**)  
2. **Wipe data / factory reset** таңдау

---

## **2.4. Download Mode-ға өту**

1. Құрылғыны өшіру  
2. **Volume Down + Volume Up + USB** басу  
3. **Volume Up** басу арқылы кіру

---

# 🪟 **3. Odin арқылы флеш жасау (Windows)**

## **3.1. Жүктеу және орнату**

- Odin жүктеу (ұсынылады 3.14.4)  
- Samsung USB драйверлерін орнату  

---

## **3.2. Odin баптау**

1. Odin іске қосу  
2. Құрылғыны қосу → **Added!!** көрсетуі тиіс  
3. **AP** → `.tar` архивін таңдау  
4. Тек осы опциялар қосулы болу керек:
   - **Auto Reboot**  
   - **F. Reset Time**  

Қоспау керек:
- Re-Partition  
- Nand Erase  

---

## **3.3. Прошивка бастау**

Odin-де **Start** басу және 1–3 минут күту.  
Сәтті болса — **PASS!** шығады.

---

## **3.4. Нәтижені тексеру**

Құрылғы автоматты түрде жүктеледі.  
Егер жүктелсе — GSI орнатылды.  
Bootloop болса — Recovery-де Wipe Data орындау.

---

# 🐧 **4. Heimdall арқылы флеш жасау (Linux/Mac)**

## **4.1. Heimdall орнату**

### Ubuntu/Debian:

```bash
sudo apt install heimdall-flash
```

### Arch Linux:

```bash
sudo pacman -S heimdall
```

### macOS (brew):

```bash
brew install heimdall
```

---

## **4.2. Файлдарды флешке дайындау**

1. `.tar` архивті жүктедіңіз.  
2. Оны ашыңыз:

```bash
tar -xf archive_name.tar
```

3. Ішінен бір немесе екі файл шығады:  
   - **boot.img** (бар болса)  
   - **super.img**

4. Осы файлдар орналасқан каталогқа өту:

```bash
cd ~/path_to_extracted_files
```

---

## **4.3. Құрылғыны тексеру**

Құрылғы **Download Mode** режимінде болуы керек:

```bash
heimdall detect
```

Егер Heimdall *Device detected* деп көрсетті — бәрі дайын.

---

## **4.4. super.img прошивкасы**

```bash
sudo heimdall flash --super super.img
```

> Кейбір Samsung құрылғыларында бөлім атауы `--super` немесе `--SUPER` болуы мүмкін.  
> Қате шықса, екеуін де байқап көріңіз.  
> Қажет болса, **boot.img** де прошивка жасауға болады:

```bash
sudo heimdall flash --boot boot.img
```

---

## **4.5. Аяқтау**

Heimdall прогресті көрсетеді және құрылғыны автоматты түрде қайта жүктейді.  

Қайта жүктелгеннен кейін құрылғы жүктелгенін және GSI орнатылғанын тексеріңіз.

---

## **4.6. Орнатуды тексеру**

- Құрылғы жүктелді → бәрі дұрыс  
- Bootloop → Wipe Data орындау  
- Флеш қатесі → GSI үйлесімділігін тексеру  

---

# 🎉 Дайын!

**Енді GSI орнатылды және пайдалануға дайын.**
